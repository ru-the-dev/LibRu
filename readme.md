# LibRu

A lightweight Lua utility library used for making World of Warcraft addons.

## Installation

1. Add/clone into your Addon folder
2. Load Load.xml in your .toc (at the top)

Once embedded the library will register itself using [LibManager](https://github.com/ru-the-dev/LibManager)